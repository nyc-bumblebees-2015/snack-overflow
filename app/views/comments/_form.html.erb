<div class="comment_form">
  <%= form_for [commentable_type, comment] do |f| %>
    <div class="content">
    <%= f.text_area :content %>
    </div>
    <%= f.submit 'Post Comment'%>
  <% end %>
</div>